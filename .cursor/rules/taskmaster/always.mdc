---
alwaysApply: true
priority: 9
---

- name: "taskmaster-always"
  description: "This rule always applies to every task, regardless of context."
  when: true
  priority: 9
  actions:
    - type: "log"
      message: "Taskmaster always rule triggered. check codebase before develop new code if the code is already written."

진행해. mcp server 적극 활용하고. check codebase before develop new code if the code is already written.
