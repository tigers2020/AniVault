# AniVault 리팩토링 진행상황

> 기준 플랜: [plan.md](plan.md)  
> 최종 갱신: 2025-03-05 (Phase 5 완료, 이후 보완: 체크리스트 검증·테스트·God Module 분리·스크립트 추가)

---

## 요약

| Phase | 상태 | 완료율 | 비고 |
|-------|------|--------|------|
| Phase 0: 준비 | ✅ 완료 | 100% | baseline 태그, pytest 통과 |
| Phase 1: God Function 해소 | ✅ 완료 | 100% | cli/helpers 포맷터 분리 |
| Phase 2: God Module 분리 | ✅ 완료 | 100% | title_matcher, subtitle_matcher, scanner 완료 |
| Phase 3: shared/constants 정리 | ✅ 완료 | 100% | cli.py 분리, 도메인별 그룹화 |
| Phase 4: shared/errors 카탈로그화 | ✅ 완료 | 100% | errors/ 패키지 분할 |
| Phase 5: Clean Architecture 도입 | ✅ 완료 | 100% | domain, app, infrastructure |

---

## Phase 0: 준비 ✅

| 작업 | 상태 | 결과 |
|------|------|------|
| `git tag refactor-baseline` | ✅ | 롤백 기준점 확보 |
| `pytest tests/` 전체 통과 | ✅ | 회귀 기준 확립 |
| `scripts/detect_circular_imports.py` | ✅ | 순환 import 현황 파악 |

---

## Phase 1: God Function 해소 ✅

### 1-1. cli/helpers/match.py

| 변경 | 파일 | 내용 |
|------|------|------|
| 추출 | `match_formatters.py` | `_format_match_table`, `_build_match_stats` 등 포맷팅/통계 함수 |
| 공통 | `format_utils.py` | 공통 포맷 유틸리티 |

### 1-2. cli/helpers/organize.py

| 변경 | 파일 | 내용 |
|------|------|------|
| 추출 | `organize_formatters.py` | 포맷팅/통계 관련 함수 분리 |

### 1-3. cli/helpers/scan.py

| 변경 | 파일 | 내용 |
|------|------|------|
| 추출 | `scan_formatters.py` | `collect_scan_data` 등 스캔 결과 포맷팅 함수 |

### 검증

- `pytest tests/unit/test_match_helpers.py` ✅
- `pytest tests/unit/test_organize_handler.py` ✅
- `pytest tests/unit/test_scan_helpers.py` ✅

---

## Phase 2: God Module 분리 ✅

### 2-1. title_matcher.py ✅

| 변경 | 파일 | 내용 |
|------|------|------|
| 추출 | `title_index.py` | `TitleIndex` 클래스, `MAX_TITLE_LENGTH` 상수 |
| 유지 | `title_matcher.py` | `TitleSimilarityMatcher` + `TitleIndex` re-export (하위 호환) |

**수정 이력**

- `TitleIndex` → `title_index.py`로 분리
- `title_matcher.py` 내 고아 코드(중복 클래스 본문) 제거

**검증**

- `tests/unit/test_title_index_*.py` ✅
- `tests/core/file_grouper/test_title_matcher_*.py` ✅

### 2-2. subtitle_matcher.py ✅

| 변경 | 파일 | 내용 |
|------|------|------|
| 추출 | `subtitle_hash.py` | `calculate_file_hash`, `HASH_CHUNK_SIZE` |
| 추출 | `subtitle_index.py` | `SubtitleIndex`, `SubtitleIndexCache`, `CachedSubtitleIndex` |
| 통합 | `index_state_manager.py` | `calculate_file_hash` → `subtitle_hash`에서 import |

**검증**

- `tests/unit/test_subtitle_*.py` ✅
- `tests/unit/test_index_state_manager.py` ✅

### 2-3. scanner.py ✅

| 변경 | 파일 | 내용 |
|------|------|------|
| 추출 | `scan_filters.py` | `has_valid_extension`, `should_include_file`, `should_skip_directory`, `filter_directories_in_place`, `process_file_entry` |
| 유지 | `scanner.py` | `DirectoryScanner` + scan_filters import (필터 로직 분리) |

**검증**

- `pytest tests/` ✅
- `scripts/detect_circular_imports.py` ✅

---

## Phase 3: shared/constants 정리 🔄

### 3-1. cli.py 분리 ✅

| 변경 | 파일 | 내용 |
|------|------|------|
| 추출 | `shared/constants/cli_formatting.py` | `CLIFormatting` (색상, format_colored_message) |
| 추출 | `shared/constants/cli_messages.py` | `CLIMessages` (Error, StatusKeys, CommandNames, Success, Info, Output) |
| 유지 | `shared/constants/cli.py` | re-export (하위 호환, 레이어 규칙 준수) |

**검증**

- `pytest tests/` ✅
- `scripts/detect_circular_imports.py` ✅

### 3-2. constants 도메인별 그룹화 ✅

| 변경 | 내용 |
|------|------|
| `matching/` | matching.py → matching/__init__.py (패키지화) |
| `cli/` | cli.py, cli_formatting.py, cli_messages.py → cli/config.py, cli/formatting.py, cli/messages.py |
| `pipeline/` | system.pipeline re-export (도메인 진입점) |

**검증**

- `pytest tests/` ✅
- `scripts/detect_circular_imports.py` ✅

---

## Phase 4: shared/errors 카탈로그화 ✅

| 변경 | 파일 | 내용 |
|------|------|------|
| 추출 | `errors/codes.py` | `ErrorCode` enum |
| 추출 | `errors/context.py` | `ErrorContextModel`, `PrimitiveContextValue`, `SAFE_DICT_MASK_KEYS`, `_coerce_primitives` |
| 추출 | `errors/exceptions.py` | `AniVaultError` 및 서브클래스 (DomainError, InfrastructureError, CliError, TypeCoercionError 등) |
| 추출 | `errors/factory.py` | `create_*` 편의 함수 |
| 통합 | `errors/__init__.py` | re-export (하위 호환) |
| 삭제 | `shared/errors.py` | 단일 파일 → 패키지로 전환 |

**검증**

- `pytest tests/` ✅
- `scripts/detect_circular_imports.py` ✅

---

## Phase 5: Clean Architecture 도입 🔄

### Step 1: domain/entities/ ✅

| 변경 | 파일 | 내용 |
|------|------|------|
| 생성 | `domain/constants/entity_validation.py` | 엔티티 검증 상수 (MIN_YEAR, MAX_YEAR 등) |
| 생성 | `domain/entities/metadata.py` | `FileMetadata`, `TMDBMatchResult` |
| 생성 | `domain/entities/parser.py` | `ParsingResult`, `ParsingAdditionalInfo` |
| 수정 | `shared/models/metadata.py` | domain.entities에서 re-export |
| 수정 | `shared/models/parser.py` | domain.entities에서 re-export |

### Step 2: domain/interfaces/ ✅

| 변경 | 파일 | 내용 |
|------|------|------|
| 생성 | `domain/interfaces/tmdb_client.py` | `TMDBClientProtocol` |
| 수정 | `shared/protocols/services.py` | domain.interfaces에서 re-export |
| 수정 | `scripts/detect_circular_imports.py` | domain 레이어 추가, (domain, shared) 예외 |

### Step 3: app/use_cases/ ✅

| 변경 | 파일 | 내용 |
|------|------|------|
| 생성 | `app/models/match_services.py` | `MatchServices` (cli.models에서 이동) |
| 생성 | `app/use_cases/match_use_case.py` | `MatchUseCase` |
| 생성 | `app/use_cases/organize_use_case.py` | `OrganizeUseCase` |
| 수정 | `cli/helpers/match.py` | MatchUseCase 사용 |
| 수정 | `cli/helpers/organize.py` | OrganizeUseCase 사용 |
| 수정 | `cli/models/match_services.py` | app.models에서 re-export |

### Step 4: infrastructure/ ✅

| 변경 | 파일 | 내용 |
|------|------|------|
| 생성 | `infrastructure/__init__.py` | services re-export |
| 생성 | `infrastructure/tmdb/` | TMDBClient 등 re-export |
| 생성 | `infrastructure/cache/` | SQLiteCacheDB re-export |
| 수정 | `scripts/detect_circular_imports.py` | app, infrastructure 레이어 추가 |

**검증**

- `pytest tests/` ✅
- `scripts/detect_circular_imports.py` ✅

---

## 완료 기준 체크리스트

- [ ] 기능 동일 (scan/match/organize 동작 불변) — 수동·통합 검증 권장
- [x] import 구조 단순화 (순환 import 0) — `python scripts/detect_circular_imports.py` 실행 결과 0
- [ ] 중복 제거 (DRY) — 코드 리뷰 권장
- [x] `ruff=0`, `mypy=0`, `pytest=0f` — ruff=0·pytest 통과 검증 (mypy는 dev 설치 후 검증)
- [ ] "다시 봐도 이해됨" (함수 30줄 이하, 모듈 400줄 이하) — God Module 후속 분리로 일부 완료 (result_converters, grouping_weights)

---

## 참조

- [리팩토링 플랜](plan.md)
- [20_task_refactor.mdc](../.cursor/rules/20_task_refactor.mdc)
- [DEVELOPMENT_PROTOCOL.md](../docs/protocols/DEVELOPMENT_PROTOCOL.md)
